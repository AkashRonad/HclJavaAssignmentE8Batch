package com.app.config;

//@Configuration
public class BookConfig {

	/*
	 * @Bean public Author author() { Author author = new Author();
	 * author.setName("Sharam"); author.setAddress("Gadag"); return author; }
	 * 
	 * @Bean(initMethod = "init", destroyMethod = "cleanUp")
	 * 
	 * @Scope("prototype") public Book book() { Book book = new Book();
	 * book.setIsbn("12345678"); book.setYear("2019"); book.setAuthor(author());
	 * return book; }
	 * 
	 * @Bean public BookDAO bookDAO() { BookDAO bookDAO = null; bookDAO = new
	 * BookDAOImpl(); return bookDAO; }
	 * 
	 */

}
